name: Check master

on:
  push:
    branches:
      - master

jobs:
  Check:
    name: Build, test, and end-to-end test
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Check out repository code
        uses: actions/checkout@v4

      - name: Cache node_modules
        id: cache-node_modules
        uses: actions/cache@v4
        with:
          path: node_modules
          key: node_modules-${{ hashFiles('yarn.lock') }}

      - name: Cache ~/.elm
        uses: actions/cache@v4
        with:
          path: ~/.elm
          key: elm-${{ hashFiles('elm.json', 'review/elm.json') }}

      - name: install yarn
        run: npm install -g yarn

      - name: yarn
        if: steps.cache-node_modules.outputs.cache-hit != 'true'
        run: yarn --frozen-lockfile

      - name: Fetch lamdera-1.3.2
        run: |
          curl https://static.lamdera.com/bin/lamdera-1.3.2-linux-x86_64 > node_modules/.bin/lamdera
          chmod +x node_modules/.bin/lamdera

      - name: Build project
        run: npm run make

      - name: Tests
        run: npm run tests

      - name: Review tests
        run: npm run review-tests

      # Plain node, no Elm involved. They stand in for the browser, which the
      # end-to-end tests run without: stuff.js writes the encryption port's bytes
      # by hand, and the service worker only exists inside a browser.
      - name: Encryption port tests
        run: npm run encryption-port-tests

      - name: Service worker tests
        run: npm run service-worker-tests

      - name: End-to-end tests
        run: npm run end-to-end-tests

      - name: Elm review
        run: npm run elm-review
  Rust:
    name: Check rust-server compiles and its tests pass
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Check out repository code
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Cargo registry and build
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            rust-server/target
          key: rust-${{ hashFiles('rust-server/Cargo.lock') }}

      - name: Check rust-server compiles
        working-directory: rust-server
        run: cargo check

      - name: Test rust-server
        working-directory: rust-server
        run: cargo test